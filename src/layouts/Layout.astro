<!doctype html>
<html lang="en" class="scroll-smooth">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width" />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <meta name="generator" content={Astro.generator} />
        <title>JMMP-Portfolio</title>
    </head>
    <body class="bg-gradient-to-b from-gray-900 via-gray-700 to-gray-900 text-white min-h-screen flex flex-col transition-all duration-1000 ease-in-out relative">
        <div class="absolute inset-0 bg-gray-950 overflow-hidden -z-10">
            <div class="absolute inset-0 bg-gradient-to-br from-rose-950/40 via-violet-950/40 to-teal-950/40" />
            <div class="absolute inset-0 opacity-20">
                <svg class="absolute w-full h-full" viewBox="0 0 100 100" preserveAspectRatio="none">
                    <path 
                        d="M0,50 C20,60 40,40 60,50 C80,60 100,40 100,50 V100 H0 Z" 
                        fill="url(#pastel-gradient-1)" 
                        class="opacity-40"
                    />
                    <path 
                        d="M0,60 C20,70 40,50 60,60 C80,70 100,50 100,60 V100 H0 Z" 
                        fill="url(#pastel-gradient-2)" 
                        class="opacity-60"
                    />
                    <path 
                        d="M0,70 C20,80 40,60 60,70 C80,80 100,60 100,70 V100 H0 Z" 
                        fill="url(#pastel-gradient-3)" 
                        class="opacity-80"
                    />
                </svg>
            </div>
        </div>
        <slot />
    </body>